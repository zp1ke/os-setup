# Starship Configuration
# Shared template across Windows/Kubuntu/macOS/NixOS

format = """
[┌─ ](bold green)$username$directory$git_branch$git_status$python$nodejs$rust$java$docker_context$package$cmd_duration
[└─](bold green)$character"""

[username]
show_always = false
format = "[$user]($style)@"

[hostname]
ssh_only = false
format = "[$hostname]($style) "
disabled = true

[directory]
truncation_length = 3
truncate_to_repo = true
format = "[$path]($style)[$read_only]($read_only_style) "

[git_branch]
symbol = ""
format = "on [$symbol$branch]($style) "

[git_status]
format = '([\[$all_status$ahead_behind\]]($style) )'

[character]
success_symbol = "[➜](bold green)"
error_symbol = "[➜](bold red)"

[cmd_duration]
min_time = 500
format = "took [$duration]($style) "

[python]
symbol = ""
format = 'via [${symbol}${pyenv_prefix}(${version} )(\($virtualenv\) )]($style)'

[nodejs]
symbol = ""
format = 'via [$symbol($version )]($style)'

[rust]
symbol = ""
format = 'via [$symbol($version )]($style)'

[java]
symbol = ""
format = 'via [$symbol($version )]($style)'

[docker_context]
symbol = " "
format = 'via [$symbol$context]($style) '

[package]
symbol = "📦 "
format = 'is [$symbol$version]($style) '

# macOS-specific: Battery status for laptops
[battery]
full_symbol = "🔋"
charging_symbol = "⚡"
discharging_symbol = "💀"

[[battery.display]]
threshold = 30
style = "bold red"
